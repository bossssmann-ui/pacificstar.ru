# Pacific Star — STATE

Текущее состояние работы. Обновляется в начале каждого рабочего PR.

## Сейчас

- **Текущая задача**: PS-00 — управляемый рабочий процесс.
- **Ветка**: `cursor/ps00-project-workflow-a54d` (от `origin/main`).
- **PR**: см. открытый PR PS-00 (docs/project + правило Cursor + исключения деплоя).
- **Исходный SHA `main`** на момент старта PS-00: `f71d68a791115a56749086dd5269da54f254dd7a`.
- **Статус**: IN_REVIEW — ожидает ревью/merge владельцем (агент не выполняет merge, см. блокировки).

## Открытые PR

- `#244` — окружение Cloud Agent (`cursor/setup-cloud-agent-environment-a54d`), DRAFT, GitGuardian pass. Внимание: снапшот/сборка содержат секрет из `.env` (см. блок безопасности) — сборку не сохранять до чистой пересборки.
- `#245` — PS-00 (`cursor/ps00-project-workflow-a54d`), ready, GitGuardian pass.
- `#246` — безопасность: прекращение отслеживания `.env` (`cursor/remove-tracked-env-secret-a54d`), ready, GitGuardian pass.

## Безопасность (.env)

- Репозиторий **публичный**; `.env` отслеживался в `main` и содержит `TELEGRAM_BOT_TOKEN` с похожим на действующий значением (не раскрывается). На сайт `.env` не публикуется (деплой исключает `.env*`), кодом токен не используется.
- Исправление — PR `#246` (untrack без переписывания истории, рабочая копия сохранена, `.env.example` дополнен).
- **Статус Telegram-секрета**: «потенциально действующий опубликованный секрет; действительность не проверялась; отзыв отложен владельцем». Проблема НЕ устранена; сборку окружения с секретом не сохранять и не считать чистой; после ротации+merge `#246` — чистая пересборка.

## Контракт проверок — СОГЛАСОВАН

Владелец утвердил три обязательные проверки перед merge: `GitGuardian Security Checks`, `PHP lint`, `HTML validation` (детали — `CI-CONTRACT.md`). Реализация — отдельный PR с `ci.yml` + исправления HTML.

## Порядок и роли (согласовано)

- Merge выполняет **владелец** вручную; агент не мёржит и не обходит ограничения (PAT и т.п.).
- Задачи — временно в `PLAN.md` (Issues недоступны).
- После merge агент проверяет публикацию (HTTP + содержимое затронутых страниц, не только код 200) и продолжает следующую готовую задачу; если нужна реплика — оставляет короткую команду продолжения.

## Остаточные блокировки

1. **🚫 Блокировка биллинга GitHub Actions (главный блокер).** CI-PR `#247` открыт, три контекста появились, но jobs Actions не стартуют: аннотация run’а — «The job was not started because your account is locked due to a billing issue.» `HTML validation` и `PHP lint` → `failure` из-за блокировки, не из-за кода (локально обе зелёные: html-validate 0 ошибок, `php -l` OK). Тот же блок затронет деплой `tilda-deploy.yml` на merge (публикация не отработает). **Действие владельца: снять блокировку в GitHub → Settings → Billing.**
2. **Принудительный запрет merge не подтверждён.** Нет `administration:read` (`403`); включение required-checks (`GitGuardian Security Checks`, `HTML validation`, `PHP lint`) в защите ветки — действие владельца (см. `CI-CONTRACT.md` → «Защита ветки»), возможно после первого успешного запуска.
3. **PS-00 не завершён** до фактического запуска рабочего процесса (первый merge по контракту + проверка публикации), что сейчас упирается в блок №1.

## Разработка при заблокированном CI (согласовано владельцем)

Пока оплачена подписка Cursor, ведём полезную работу над сайтом, не дожидаясь снятия блокировки Actions. Блок биллинга — блокировка **удалённых проверок и публикации**, а не всей разработки. Для каждой задачи: обязательны локальные проверки; статус `READY_FOR_CI`, если локально готово, но Actions заблокированы; такие задачи не VERIFIED и не опубликованы.

Цепочка веток (зависимые PR, база указана явно):
- `#246` (untrack `.env`) — база security.
- `#247` (CI + HTML-фиксы) — база CI.
- `#248` **PS-01 реквизиты** — база `#247` (в рабочую ветку смёржен #246); `READY_FOR_CI`.
- `#249` **PS-02 противоречия/обещания** — база `#248`; `READY_FOR_CI`.
- `#250` **PS-03 надёжность форм** — база `#249`; `READY_FOR_CI`.
- `#251` **PS-04 аналитика** — база `#250`; `READY_FOR_CI`.
- `#252` **PS-05 цены/запрос ставки** — база `#251`; `READY_FOR_CI`.
- `#253` **PS-06 загрузка** — база `#252`; `READY_FOR_CI` (заставка/курсор/шрифты/no-JS header).

**PS-01 — локально завершён** (PR #248): реквизиты (вкл. редакцию владельца от 8.09: Сбербанк, индекс 692906), 2-й ЭДО ID, разделение продаж/курьера, печатная карточка. Локально проверено.

**PS-02 — локально завершён** (PR #249, база #248): устранены противоречия «только собственный транспорт»/«без посредников»/«не зависим от сторонних» → «собственный и привлечённый транспорт» (владелец подтвердил); убраны неподтверждённые показатели (850 клиентов, 15 000 рейсов, 10 000 м², 20+ единиц, «0 повреждений»); нейтрализованы обещания сроков (15 мин / 2 ч / 24 ч / 48 ч / экспресс 24–72 ч) и круглосуточной работы (горячая линия/поддержка/менеджер 24/7) — **во всех 5 локалях и HTML**. Сохранены честные утверждения: онлайн-инструменты работают круглосуточно (форма/ЛК/калькулятор) и охрана склада. Заявлений о статусе оператора Северного завоза и отзывов без источника нет. Проверено: html-validate 0, JSON валиден, нет пустых карточек/осиротевших счётчиков, desktop/mobile/EN проверены.
- В **PS-11 (i18n)** остаётся только **общая доработка переводов и языкового SEO** (не смысловые обещания — они устранены здесь).

## Порядок будущего выпуска (согласовано)

**`#247` (CI) → `#246` (security `.env`) → `#248` (PS-01) → `#249` (PS-02)**. `#244` (окружение) и `#245` (docs) — отдельно, со своими зависимостями (#244 — после чистой пересборки без секрета). Перед каждым merge: актуализация ветки + три успешные проверки на её окончательной ревизии. Merge и публикацию сейчас не выполняем (блок биллинга Actions).

**PS-03 — локально завершён** (PR #250, база #249): PHP-обработчики `api/*.php` проверены (php-cli): серверная валидация обязательных полей (400), нет ложного успеха при сбое отправки (500), метод-гард (405), в логах нет секретов/содержимого заявок. Фронт (`main.js`) усилен: успех только при `data.ok===true`; явный in-flight guard против двойной отправки; ввод сохраняется при ошибке. Доведены остаточные обещания в JS (callback «5 минут», hero «15 минут» fallback). Различаем приём обработчиком / приём транспортом / доставку — **доставку в ящик не подтверждаем без тестового почтового канала** (нужен тестовый ящик + рабочий SMTP/секреты).

## План v1.2 (08.09.2026) — принято

Полный план — `docs/project/PacificStar-Cursor-Plan.md` (v1.2; раздел 11 приоритетен). Добавлены PS-07A (бренд/Canva, направление D «Печать Меридиана»), PS-14A (карта страниц/спека бренда/регламент новостей + аудит App Platform #220769), PS-16A–C (новости), дополнения к PS-03/04/10/11/12 (см. PLAN.md). Старые документы System-A — требования/история, не доказательство текущего состояния; для состояния кода источник истины — актуальный GitHub. Telegram-секрет: не включать в новые артефакты/сборки; отзыв отложен владельцем.

**PS-04 — локально завершён** (PR #251): события `form_start`/`form_error`/`lead_accepted`/`calculator_used`/`phone_click`/`messenger_click` разделены; `lead_accepted` только при `ok:true`; ПДн (номер/e-mail/имя/текст) в аналитику не передаются; consent-gated; калькулятор ≠ лид. Проверено Playwright с моком транспорта (реальные заявки не слались). Кабинет Метрики `110523171` — **действие владельца** (создать/сверить цели; миграция `form_submit`→`lead_accepted`+`form_start`); GA — только при подтверждении.

**PS-05 — локально завершён** (PR #252): калькулятор → «Запрос ставки» (без ₽, переносит маршрут/параметры в форму); тарифы «По запросу»; убраны «100 кг/м³ что меньше» и примеры цен (₽/USD) во всех локалях. Проверено Playwright. Числовой расчёт вернётся в PS-13.

**PS-05 передача данных доработана** (PR #252): маршрут/груз больше не в URL — краткоживущий `sessionStorage` (read-once + clear, `.value`, устойчиво к недоступному/просроченному хранилищу). Смёржено в #253 для наследования. Проверено Playwright.

**PS-06 локально готов** (PR #253): (1) незаблокирующая заставка (DOM-ready + таймаут + noscript/CSS fail-safe); (2) убран декоративный курсор; (3) неблокирующая загрузка шрифтов (`media=print/onload`+swap+noscript) — локальный замер под медленным шрифт-сервером: H1 ~4237→~153 мс; (4) no-JS header-fallback (nav + телефон + email; при JS — один header, без дублей); (5) изображения главной уже ок (width + lazy, hero fetchpriority). Проверено Playwright (JS on/off). **hero-форма без JS не отправляется — доступный контакт без JS обеспечен телефоном/e-mail в fallback.**
- Остаток PS-06 (опционально): self-host шрифтов (Inter OFL, убрать сторонний запрос), отправка форм без JS. Полевые CWV — после публикации.

## Уточнение PS-04 (ограничение проверки)
- Мок `window.ym` подтверждает **логику наших событий**, но НЕ отсутствие утечек через автоматически собираемый Метрикой URL и НЕ реальную конфигурацию счётчика. Кабинет `110523171` — действие владельца. **Миграция целей:** старая цель `form_submit` (фиксировала попытку/калькулятор/успех вместе) заменяется на `form_start` (попытка) + `lead_accepted` (успех, только `ok:true`) + `calculator_used` (калькулятор). Кабинетную настройку по этим именам нельзя терять.

## Следующая задача

- **PS-16A** (по плану v1.2): сначала сверить `news.html`, `news/*.html`, issue #228 и связанные PR; проверить первоисточники, даты и утверждения; НЕ создавать новые новости ради количества; материалы к публикации — редакционное ревью Романа. Зависимой веткой от `#253`.
- Дизайн-реализация PS-07/07A/08–10 — после DESIGN_READY (нужны исходники логотипов + раздел 3 брендбука).
- **Правило очереди (11.7):** не переключаться на новости (PS-16), пока не закрыты формы/аналитика/калькулятор/загрузка, кроме отдельно обоснованной критической ошибки. Новости (PS-16C) — только с редакционным ревью Романа перед merge.
- Бренд/Canva (PS-07A) — можно готовить спецификацию; визуальная реализация после DESIGN_READY (нужны 2 SVG логотипа + раздел 3 брендбука).

## Команды продолжения

```text
Прочитать docs/project/{PLAN,FACTS,STATE,CI-CONTRACT,DECISIONS}.md и .github/copilot-instructions.md.
Сверить состояние с GitHub (open PR/issues, HEAD, проверки). Возобновить незавершённую задачу без дублей.
Локальные проверки: npm install; npm run validate; npm run spellcheck; npm start (сервер на :3000).
```
