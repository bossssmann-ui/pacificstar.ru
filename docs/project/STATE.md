# Pacific Star — STATE

Текущее состояние работы. Обновляется в начале каждого рабочего PR.

## Сейчас

- **Текущая задача**: PS-00 — управляемый рабочий процесс.
- **Ветка**: `cursor/ps00-project-workflow-a54d` (от `origin/main`).
- **PR**: см. открытый PR PS-00 (docs/project + правило Cursor + исключения деплоя).
- **Исходный SHA `main`** на момент старта PS-00: `f71d68a791115a56749086dd5269da54f254dd7a`.
- **Статус**: IN_REVIEW — ожидает ревью/merge владельцем (агент не выполняет merge, см. блокировки).

## Открытые PR

- `#244` — окружение Cloud Agent (`cursor/setup-cloud-agent-environment-a54d`), DRAFT, GitGuardian pass. Внимание: снапшот/сборка содержат секрет из `.env` (см. блок безопасности) — сборку не сохранять до чистой пересборки.
- `#245` — PS-00 (`cursor/ps00-project-workflow-a54d`), ready, GitGuardian pass.
- `#246` — безопасность: прекращение отслеживания `.env` (`cursor/remove-tracked-env-secret-a54d`), ready, GitGuardian pass.

## Безопасность (.env)

- Репозиторий **публичный**; `.env` отслеживался в `main` и содержит `TELEGRAM_BOT_TOKEN` с похожим на действующий значением (не раскрывается). На сайт `.env` не публикуется (деплой исключает `.env*`), кодом токен не используется.
- Исправление — PR `#246` (untrack без переписывания истории, рабочая копия сохранена, `.env.example` дополнен).
- **ОТКРЫТЫЙ РИСК**: ротацию токена владелец **отложил**. Проблема НЕ устранена — значение остаётся в истории публичного репо и валидно, пока не отозвано через `@BotFather`. Сборку окружения с секретом не сохранять и не считать чистой; после ротации+merge `#246` — чистая пересборка.

## Блокировки

1. **«Три проверки» не найдены исторически.** На всех PR (вкл. #200, проверен по всем коммитам) — только `GitGuardian`; Timeweb Deploy — после merge. Нет `pull_request`-workflow; защита ветки недоступна токену (`403`, нужно `administration:read`). Требуется: список required-checks из UI (`Settings → Branches → main`) или `administration:read`, ЛИБО согласование предложенного контракта из `CI-CONTRACT.md` (GitGuardian + html-validate + php -l, + проверка публикации). **До согласования merge не выполнять.**
2. **Создание GitHub Issues недоступно в рамках правил среды.** `gh`/HTTP — только чтение; инструмента создания Issues нет; PAT в Secrets тоже не даёт штатного канала записи (использование через `gh`/`curl` — запрещённый обход). `PLAN.md` — рабочий трекер. Требуется решение владельца о канале Issues.
3. **Merge не исполним агентом в этой среде.** Нет инструмента merge (`gh` read-only; `ManagePullRequest` не мёржит). Разрешение владельца на самомёрж это не меняет — фактический merge и проверку публикации выполняет владелец.

## Следующая задача

- После снятия блокировок и завершения PS-00 → **PS-01** (реквизиты, данные в `FACTS.md`). Технические задачи (**PS-06**) можно готовить, но не выпускать независимо, пока контракт merge не установлен.

## Команды продолжения

```text
Прочитать docs/project/{PLAN,FACTS,STATE,CI-CONTRACT,DECISIONS}.md и .github/copilot-instructions.md.
Сверить состояние с GitHub (open PR/issues, HEAD, проверки). Возобновить незавершённую задачу без дублей.
Локальные проверки: npm install; npm run validate; npm run spellcheck; npm start (сервер на :3000).
```
