# Pacific Star — STATE

Текущее состояние работы. Обновляется в начале каждого рабочего PR.

## Сейчас

- **Текущая задача**: PS-00 — управляемый рабочий процесс.
- **Ветка**: `cursor/ps00-project-workflow-a54d` (от `origin/main`).
- **PR**: см. открытый PR PS-00 (docs/project + правило Cursor + исключения деплоя).
- **Исходный SHA `main`** на момент старта PS-00: `f71d68a791115a56749086dd5269da54f254dd7a`.
- **Статус**: IN_REVIEW — ожидает ревью/merge владельцем (агент не выполняет merge, см. блокировки).

## Блокировки

1. **Создание GitHub Issues недоступно** в среде Cloud Agent: `gh` доступен только на чтение, инструмента создания issues нет. Задачи PS-XX ведутся в `PLAN.md`. Требуется: доступ на запись к Issues или подтверждение, что трекинг в репозитории достаточен.
2. **Автоматический merge агентом не выполняется**. Merge в `main` автоматически публикует сайт на Timeweb (релиз). Merge оставлен владельцу после ревью. Требуется: решение владельца о том, кто выполняет merge.
3. **«Три проверки» не подтверждены**. На PR фактически выполняется одна проверка — `GitGuardian Security Checks` (GitHub App). Защита ветки `main` недоступна токену (HTTP 403), rulesets пусты. Требуется: точные названия трёх проверок и/или доступ к настройкам защиты ветки. См. `CI-CONTRACT.md`.

## Следующая задача

- **PS-01** (реквизиты) — READY: данные подтверждены в `FACTS.md`, зависит только от PS-00. Либо **PS-06** (технические перф/доступность) — READY, не требует Canva.

## Команды продолжения

```text
Прочитать docs/project/{PLAN,FACTS,STATE,CI-CONTRACT,DECISIONS}.md и .github/copilot-instructions.md.
Сверить состояние с GitHub (open PR/issues, HEAD, проверки). Возобновить незавершённую задачу без дублей.
Локальные проверки: npm install; npm run validate; npm run spellcheck; npm start (сервер на :3000).
```
