# Node.js dev dependencies (html-validate etc.) — NOT part of the website
node_modules/
package-lock.json

# macOS
.DS_Store

# Editor artefacts — keep shared extension recommendations, ignore everything else
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# Build output (if added later)
dist/
build/

# Environment / secrets
.env
.env.local
.env.*.local
api/mail-config.php
map-test.html

# Raw photo uploads from client (keep folder, ignore heavy files)
content-inbox/photos/*
!content-inbox/photos/.gitkeep
